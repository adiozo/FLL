# Third-party notices

The `marugoto_adapter/` modules are adaptations of routines from the Marugoto project. Before public release,
identify the exact upstream repository and revision used, review its license, retain every required copyright
and attribution notice, and confirm that the intended repository license is compatible.

Pretrained torchvision weights are downloaded under their respective upstream terms. Cite the model and
training dataset used in the paper and retain the model metadata emitted by the feature-extraction command.

This file is a release checklist, not a substitute for the upstream license texts.
